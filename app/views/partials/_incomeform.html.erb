<%= simple_form_for(@transactions) do |f| %>

  <%= f.number_field :amount, step: 10_000, placeholder: "Income", required: true, label: false, class: "my-4 w-100" %>

  <%= f.button :submit, "Add Income", class: "btn btn-primary"%>
<% end %>
